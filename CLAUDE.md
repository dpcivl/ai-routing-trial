# CLAUDE.md

로컬 LLM 라우팅 학습 프로젝트 (Python + uv, Ollama, 공개 GitHub 레포). 구조와 설계 결정은 [docs/architecture.md](docs/architecture.md), 환경은 [docs/setup.md](docs/setup.md) 참고.

## 작업 방식 (반드시 지킴)
- **필수 도구 먼저 확인:** 코드를 짜기 전에 `which ollama`, 서버 응답, 필요한 모델 설치 여부를 확인한다. 없으면 설치·실행을 유저에게 요청하고, 완료했다는 말이 있을 때까지 코드를 짜지 않는다. sudo·비밀번호 단계는 유저가 직접 한다.
- **이해가 먼저:** 한 단계를 구현하면 멈추고 아키텍처 수준으로 설명한다(그림·표·임베디드 비유). 유저가 이해했다고 말하기 전에는 다음 코드를 짜지 않고, 다음 단계를 미리 만들어 두지도 않는다.
- **커밋은 항상 작업 단위로 나눈다.** 이해 여부와 무관하다. 메시지는 `feat:` `docs:` `chore:` 형식. push는 유저가 요청할 때만 한다.
- **검증하지 않은 것을 검증했다고 쓰지 않는다.** 쓰지 않은 스킬·도구를 쓴 것처럼 적지 않는다. 출처가 모호한 수치는 그렇다고 표시한다.

## 유저 맥락
- 임베디드 개발자 출신이고 코드 읽기는 가능하지만 학습 단계다. 한국어로, 구체적으로, 임베디드 비유를 섞어 설명한다. 코드에는 설명 주석을 충분히 단다.
- 문서를 많이 만든다. 템플릿은 `docs/templates/`. `docs/routing/`의 자동 생성 보고서는 직접 고치지 않는다.

## 프로젝트 규칙
- 공개 레포이므로 모델 파일, API 키, 회사 정보를 커밋하지 않는다.
- 하드웨어는 M1 16GB. 모델 하나에 약 10GB 이하만 쓰고 한 번에 하나만 올린다. 회사는 vLLM을 쓰므로 백엔드는 OpenAI 호환 API를 유지한다.
- 명령: `uv sync` / `uv run pytest` / `uv run ruff check src tests` / `uv run ai-router --help`
